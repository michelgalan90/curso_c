#include <stdio.h>

int main(){
    /*
    las variables locales se ejecutan en el stack, y se van apilando a medida que se conectan las
    funciones una tras otra.
    las variables globales, y non-auto, static, se almacenan en la memoria heap y duran ahi
    durante toda la ejecucion del programa.
    */

    return 0;
}